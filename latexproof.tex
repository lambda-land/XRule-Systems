Build profile: -w ghc-9.4.1 -O1
In order, the following will be built (use -v for more details):
 - xrs-0.1.0.0 (exe:XRuleSystems) (file app/Main.hs changed)
Preprocessing executable 'XRuleSystems' for xrs-0.1.0.0..
Building executable 'XRuleSystems' for xrs-0.1.0.0..
[1 of 2] Compiling Main             ( app/Main.hs, /Users/iainmoncrief/Documents/Github/XRule-Systems/dist-newstyle/build/x86_64-osx/ghc-9.4.1/xrs-0.1.0.0/x/XRuleSystems/build/XRuleSystems/XRuleSystems-tmp/Main.o ) [Source file changed]
[2 of 2] Linking /Users/iainmoncrief/Documents/Github/XRule-Systems/dist-newstyle/build/x86_64-osx/ghc-9.4.1/xrs-0.1.0.0/x/XRuleSystems/build/XRuleSystems/XRuleSystems [Objects changed]
letrec length = $\lambda$ xs -> if xs == [] then 0 else 1 + length (tail xs) in letrec minimum = $\lambda$ xs -> if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m in minimum [2,3,4,1,5]
\{  \} : \code{letrec length = $\lambda$ xs -> if xs == [] then 0 else 1 + length (tail xs) in letrec minimum = $\lambda$ xs -> if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m in minimum [2,3,4,1,5]} \Rightarrow \code{1}
\infer[]{\{  \} : \code{minimum [2,3,4,1,5]} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{xs == []} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{xs} \Rightarrow \code{[2,3,4,1,5]} && \{  \} : \code{[2,3,4,1,5] == []} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{length xs == 1} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{length xs} \Rightarrow \code{5} && \{  \} : \code{5 == 1} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{head xs} \Rightarrow \code{2} && \infer[]{\{ \code{h} \mapsto \code{2} , \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{minimum (tail xs)} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[2,3,4,1,5]} \} : \code{tail xs} \Rightarrow \code{[3,4,1,5]} && \infer[]{\{ \code{xs} \mapsto \code{[3,4,1,5]} , \code{h} \mapsto \code{2} \} : \code{if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{xs == []} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{xs} \Rightarrow \code{[3,4,1,5]} && \{  \} : \code{[3,4,1,5] == []} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[3,4,1,5]} , \code{h} \mapsto \code{2} \} : \code{if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{length xs == 1} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{length xs} \Rightarrow \code{4} && \{  \} : \code{4 == 1} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[3,4,1,5]} , \code{h} \mapsto \code{2} \} : \code{let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{head xs} \Rightarrow \code{3} && \infer[]{\{ \code{h} \mapsto \code{3} , \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{minimum (tail xs)} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[3,4,1,5]} \} : \code{tail xs} \Rightarrow \code{[4,1,5]} && \infer[]{\{ \code{xs} \mapsto \code{[4,1,5]} , \code{h} \mapsto \code{3} \} : \code{if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{xs == []} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{xs} \Rightarrow \code{[4,1,5]} && \{  \} : \code{[4,1,5] == []} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[4,1,5]} , \code{h} \mapsto \code{3} \} : \code{if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{length xs == 1} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{length xs} \Rightarrow \code{3} && \{  \} : \code{3 == 1} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[4,1,5]} , \code{h} \mapsto \code{3} \} : \code{let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{head xs} \Rightarrow \code{4} && \infer[]{\{ \code{h} \mapsto \code{4} , \code{xs} \mapsto \code{[4,1,5]} \} : \code{let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{minimum (tail xs)} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[4,1,5]} \} : \code{tail xs} \Rightarrow \code{[1,5]} && \infer[]{\{ \code{xs} \mapsto \code{[1,5]} , \code{h} \mapsto \code{4} \} : \code{if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{xs == []} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{xs} \Rightarrow \code{[1,5]} && \{  \} : \code{[1,5] == []} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[1,5]} , \code{h} \mapsto \code{4} \} : \code{if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{length xs == 1} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{length xs} \Rightarrow \code{2} && \{  \} : \code{2 == 1} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[1,5]} , \code{h} \mapsto \code{4} \} : \code{let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{head xs} \Rightarrow \code{1} && \infer[]{\{ \code{h} \mapsto \code{1} , \code{xs} \mapsto \code{[1,5]} \} : \code{let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{minimum (tail xs)} \Rightarrow \code{5}}{\{ \code{xs} \mapsto \code{[1,5]} \} : \code{tail xs} \Rightarrow \code{[5]} && \infer[]{\{ \code{xs} \mapsto \code{[5]} , \code{h} \mapsto \code{1} \} : \code{if xs == [] then 0 else if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{5}}{\infer[]{\{ \code{xs} \mapsto \code{[5]} \} : \code{xs == []} \Rightarrow \code{False}}{\{ \code{xs} \mapsto \code{[5]} \} : \code{xs} \Rightarrow \code{[5]} && \{  \} : \code{[5] == []} \Rightarrow \code{False}} && \infer[]{\{ \code{xs} \mapsto \code{[5]} , \code{h} \mapsto \code{1} \} : \code{if length xs == 1 then head xs else let h = head xs in let m = minimum (tail xs) in if h <= m then h else m} \Rightarrow \code{5}}{\infer[]{\{ \code{xs} \mapsto \code{[5]} \} : \code{length xs == 1} \Rightarrow \code{True}}{\{ \code{xs} \mapsto \code{[5]} \} : \code{length xs} \Rightarrow \code{1} && \{  \} : \code{1 == 1} \Rightarrow \code{True}} && \{ \code{xs} \mapsto \code{[5]} \} : \code{head xs} \Rightarrow \code{5}}}} && \infer[]{\{ \code{m} \mapsto \code{5} , \code{h} \mapsto \code{1} \} : \code{if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{m} \mapsto \code{5} , \code{h} \mapsto \code{1} \} : \code{h <= m} \Rightarrow \code{True}}{\{ \code{h} \mapsto \code{1} \} : \code{h} \Rightarrow \code{1} && \{ \code{m} \mapsto \code{5} \} : \code{m} \Rightarrow \code{5} && \{  \} : \code{1 <= 5} \Rightarrow \code{True}} && \{ \code{h} \mapsto \code{1} \} : \code{h} \Rightarrow \code{1}}}}}}} && \infer[]{\{ \code{m} \mapsto \code{1} , \code{h} \mapsto \code{4} \} : \code{if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{m} \mapsto \code{1} , \code{h} \mapsto \code{4} \} : \code{h <= m} \Rightarrow \code{False}}{\{ \code{h} \mapsto \code{4} \} : \code{h} \Rightarrow \code{4} && \{ \code{m} \mapsto \code{1} \} : \code{m} \Rightarrow \code{1} && \{  \} : \code{4 <= 1} \Rightarrow \code{False}} && \{ \code{m} \mapsto \code{1} \} : \code{m} \Rightarrow \code{1}}}}}}} && \infer[]{\{ \code{m} \mapsto \code{1} , \code{h} \mapsto \code{3} \} : \code{if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{m} \mapsto \code{1} , \code{h} \mapsto \code{3} \} : \code{h <= m} \Rightarrow \code{False}}{\{ \code{h} \mapsto \code{3} \} : \code{h} \Rightarrow \code{3} && \{ \code{m} \mapsto \code{1} \} : \code{m} \Rightarrow \code{1} && \{  \} : \code{3 <= 1} \Rightarrow \code{False}} && \{ \code{m} \mapsto \code{1} \} : \code{m} \Rightarrow \code{1}}}}}}} && \infer[]{\{ \code{m} \mapsto \code{1} , \code{h} \mapsto \code{2} \} : \code{if h <= m then h else m} \Rightarrow \code{1}}{\infer[]{\{ \code{m} \mapsto \code{1} , \code{h} \mapsto \code{2} \} : \code{h <= m} \Rightarrow \code{False}}{\{ \code{h} \mapsto \code{2} \} : \code{h} \Rightarrow \code{2} && \{ \code{m} \mapsto \code{1} \} : \code{m} \Rightarrow \code{1} && \{  \} : \code{2 <= 1} \Rightarrow \code{False}} && \{ \code{m} \mapsto \code{1} \} : \code{m} \Rightarrow \code{1}}}}}}}
