-- File generated by the BNF Converter (bnfc 2.9.4.1).

{-# LANGUAGE CPP #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE LambdaCase #-}
#if __GLASGOW_HASKELL__ <= 708
{-# LANGUAGE OverlappingInstances #-}
#endif

-- | Pretty-printer for PrintParser.

module PrintParser where

import Prelude
  ( ($), (.)
  , Bool(..), (==), (<)
  , Int, Integer, Double, (+), (-), (*)
  , String, (++)
  , ShowS, showChar, showString
  , all, elem, foldr, id, map, null, replicate, shows, span
  )
import Data.Char ( Char, isSpace )
import qualified AbsParser

-- | The top-level printing method.

printTree :: Print a => a -> String
printTree = render . prt 0

type Doc = [ShowS] -> [ShowS]

doc :: ShowS -> Doc
doc = (:)

render :: Doc -> String
render d = rend 0 False (map ($ "") $ d []) ""
  where
  rend
    :: Int        -- ^ Indentation level.
    -> Bool       -- ^ Pending indentation to be output before next character?
    -> [String]
    -> ShowS
  rend i p = \case
      "["      :ts -> char '[' . rend i False ts
      "("      :ts -> char '(' . rend i False ts
      "{"      :ts -> onNewLine i     p . showChar   '{'  . new (i+1) ts
      "}" : ";":ts -> onNewLine (i-1) p . showString "};" . new (i-1) ts
      "}"      :ts -> onNewLine (i-1) p . showChar   '}'  . new (i-1) ts
      [";"]        -> char ';'
      ";"      :ts -> char ';' . new i ts
      t  : ts@(s:_) | closingOrPunctuation s
                   -> pending . showString t . rend i False ts
      t        :ts -> pending . space t      . rend i False ts
      []           -> id
    where
    -- Output character after pending indentation.
    char :: Char -> ShowS
    char c = pending . showChar c

    -- Output pending indentation.
    pending :: ShowS
    pending = if p then indent i else id

  -- Indentation (spaces) for given indentation level.
  indent :: Int -> ShowS
  indent i = replicateS (2*i) (showChar ' ')

  -- Continue rendering in new line with new indentation.
  new :: Int -> [String] -> ShowS
  new j ts = showChar '\n' . rend j True ts

  -- Make sure we are on a fresh line.
  onNewLine :: Int -> Bool -> ShowS
  onNewLine i p = (if p then id else showChar '\n') . indent i

  -- Separate given string from following text by a space (if needed).
  space :: String -> ShowS
  space t s =
    case (all isSpace t', null spc, null rest) of
      (True , _   , True ) -> []              -- remove trailing space
      (False, _   , True ) -> t'              -- remove trailing space
      (False, True, False) -> t' ++ ' ' : s   -- add space if none
      _                    -> t' ++ s
    where
      t'          = showString t []
      (spc, rest) = span isSpace s

  closingOrPunctuation :: String -> Bool
  closingOrPunctuation [c] = c `elem` closerOrPunct
  closingOrPunctuation _   = False

  closerOrPunct :: String
  closerOrPunct = ")],;"

parenth :: Doc -> Doc
parenth ss = doc (showChar '(') . ss . doc (showChar ')')

concatS :: [ShowS] -> ShowS
concatS = foldr (.) id

concatD :: [Doc] -> Doc
concatD = foldr (.) id

replicateS :: Int -> ShowS -> ShowS
replicateS n f = concatS (replicate n f)

-- | The printer class does the job.

class Print a where
  prt :: Int -> a -> Doc

instance {-# OVERLAPPABLE #-} Print a => Print [a] where
  prt i = concatD . map (prt i)

instance Print Char where
  prt _ c = doc (showChar '\'' . mkEsc '\'' c . showChar '\'')

instance Print String where
  prt _ = printString

printString :: String -> Doc
printString s = doc (showChar '"' . concatS (map (mkEsc '"') s) . showChar '"')

mkEsc :: Char -> Char -> ShowS
mkEsc q = \case
  s | s == q -> showChar '\\' . showChar s
  '\\' -> showString "\\\\"
  '\n' -> showString "\\n"
  '\t' -> showString "\\t"
  s -> showChar s

prPrec :: Int -> Int -> Doc -> Doc
prPrec i j = if j < i then parenth else id

instance Print Integer where
  prt _ x = doc (shows x)

instance Print Double where
  prt _ x = doc (shows x)

instance Print AbsParser.TokUnit where
  prt _ (AbsParser.TokUnit (_,i)) = doc $ showString i
instance Print AbsParser.UIdent where
  prt _ (AbsParser.UIdent (_,i)) = doc $ showString i
instance Print AbsParser.PIdent where
  prt _ (AbsParser.PIdent (_,i)) = doc $ showString i
instance Print AbsParser.PInteger where
  prt _ (AbsParser.PInteger (_,i)) = doc $ showString i
instance Print AbsParser.SCPL where
  prt i = \case
    AbsParser.SCPLPROG defns -> prPrec i 0 (concatD [prt 0 defns])

instance Print [AbsParser.Defn] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, prt 0 xs]

instance Print AbsParser.Defn where
  prt i = \case
    AbsParser.DEFN pident stmt -> prPrec i 0 (concatD [prt 0 pident, doc (showString "="), prt 1 stmt])

instance Print AbsParser.Stmt where
  prt i = \case
    AbsParser.CASESTMT exp caseterms -> prPrec i 2 (concatD [doc (showString "case"), prt 0 exp, doc (showString "of"), doc (showString "{"), prt 0 caseterms, doc (showString "}")])
    AbsParser.IFSTMT exp stmt1 stmt2 -> prPrec i 2 (concatD [doc (showString "if"), prt 0 exp, doc (showString "then"), prt 2 stmt1, doc (showString "else"), prt 2 stmt2])
    AbsParser.BARESTMT exp -> prPrec i 2 (concatD [prt 0 exp])
    AbsParser.ELet let_ letinstmt stmt -> prPrec i 1 (concatD [prt 0 let_, prt 0 letinstmt, doc (showString "in"), prt 1 stmt])

instance Print AbsParser.Let where
  prt i = \case
    AbsParser.LET -> prPrec i 0 (concatD [doc (showString "let")])
    AbsParser.LRec -> prPrec i 0 (concatD [doc (showString "letrec")])

instance Print AbsParser.CaseTerm where
  prt i = \case
    AbsParser.CASE_STMT casepattern stmt -> prPrec i 0 (concatD [prt 0 casepattern, doc (showString "->"), prt 1 stmt])

instance Print AbsParser.LetInStmt where
  prt i = \case
    AbsParser.LET_IN_STMT pident exp -> prPrec i 0 (concatD [prt 0 pident, doc (showString "="), prt 0 exp])

instance Print AbsParser.Type where
  prt i = \case
    AbsParser.TYPEARROW typen type_ -> prPrec i 0 (concatD [prt 0 typen, doc (showString "->"), prt 0 type_])
    AbsParser.TYPENext typen -> prPrec i 0 (concatD [prt 0 typen])

instance Print AbsParser.TypeN where
  prt i = \case
    AbsParser.TYPEUNIT tokunit -> prPrec i 0 (concatD [prt 0 tokunit])
    AbsParser.TYPECONST_VAR uident -> prPrec i 0 (concatD [prt 0 uident])
    AbsParser.TYPELIST type_ -> prPrec i 0 (concatD [doc (showString "["), prt 0 type_, doc (showString "]")])
    AbsParser.TYPEPROD types -> prPrec i 0 (concatD [doc (showString "<"), prt 0 types, doc (showString ">")])
    AbsParser.TYPEBRACKET type_ -> prPrec i 0 (concatD [doc (showString "("), prt 0 type_, doc (showString ")")])
    AbsParser.CONST_TYPE constanttype -> prPrec i 0 (concatD [prt 0 constanttype])

instance Print [AbsParser.TypeN] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [AbsParser.Type] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print [AbsParser.UIdent] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print AbsParser.CasePattern where
  prt i = \case
    AbsParser.TRUE_PATTERN -> prPrec i 0 (concatD [doc (showString "True")])
    AbsParser.FALSE_PATTERN -> prPrec i 0 (concatD [doc (showString "False")])

instance Print [AbsParser.CaseTerm] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ";"), prt 0 xs]

instance Print AbsParser.Exp where
  prt i = \case
    AbsParser.EInt pinteger -> prPrec i 15 (concatD [prt 0 pinteger])
    AbsParser.ETrue -> prPrec i 15 (concatD [doc (showString "True")])
    AbsParser.EFalse -> prPrec i 15 (concatD [doc (showString "False")])
    AbsParser.EVar pident -> prPrec i 15 (concatD [prt 0 pident])
    AbsParser.EString str -> prPrec i 15 (concatD [printString str])
    AbsParser.EList exps -> prPrec i 15 (concatD [doc (showString "["), prt 0 exps, doc (showString "]")])
    AbsParser.ECall pident exps -> prPrec i 15 (concatD [prt 0 pident, doc (showString "("), prt 0 exps, doc (showString ")")])
    AbsParser.ENeg exp -> prPrec i 13 (concatD [doc (showString "-"), prt 14 exp])
    AbsParser.CONSTERM exp1 exp2 -> prPrec i 13 (concatD [prt 13 exp1, doc (showString ":"), prt 14 exp2])
    AbsParser.EAPPEND exp1 exp2 -> prPrec i 12 (concatD [prt 12 exp1, doc (showString "++"), prt 13 exp2])
    AbsParser.EMul exp1 exp2 -> prPrec i 12 (concatD [prt 12 exp1, doc (showString "*"), prt 13 exp2])
    AbsParser.EDiv exp1 exp2 -> prPrec i 12 (concatD [prt 12 exp1, doc (showString "/"), prt 13 exp2])
    AbsParser.EAdd exp1 exp2 -> prPrec i 11 (concatD [prt 11 exp1, doc (showString "+"), prt 12 exp2])
    AbsParser.ESub exp1 exp2 -> prPrec i 11 (concatD [prt 11 exp1, doc (showString "-"), prt 12 exp2])
    AbsParser.ELt exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString "<"), prt 10 exp2])
    AbsParser.EGt exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString ">"), prt 10 exp2])
    AbsParser.ELEq exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString "<="), prt 10 exp2])
    AbsParser.EGEq exp1 exp2 -> prPrec i 9 (concatD [prt 9 exp1, doc (showString ">="), prt 10 exp2])
    AbsParser.EEq exp1 exp2 -> prPrec i 8 (concatD [prt 8 exp1, doc (showString "=="), prt 9 exp2])
    AbsParser.ENEq exp1 exp2 -> prPrec i 8 (concatD [prt 8 exp1, doc (showString "!="), prt 9 exp2])
    AbsParser.EAnd exp1 exp2 -> prPrec i 4 (concatD [prt 4 exp1, doc (showString "&&"), prt 5 exp2])
    AbsParser.EOr exp1 exp2 -> prPrec i 3 (concatD [prt 3 exp1, doc (showString "||"), prt 4 exp2])
    AbsParser.ELambda pidents stmt -> prPrec i 1 (concatD [doc (showString "\\"), prt 0 pidents, doc (showString "->"), prt 1 stmt])

instance Print [AbsParser.PIdent] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x, doc (showString " ")]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString " "), prt 0 xs]

instance Print AbsParser.TypeAnnotation where
  prt i = \case
    AbsParser.TYPEANNOTATION type_ -> prPrec i 0 (concatD [doc (showString "::"), prt 0 type_])
    AbsParser.TYPEANNOTATION_EMPTY -> prPrec i 0 (concatD [])

instance Print [AbsParser.Exp] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print AbsParser.ConstantType where
  prt i = \case
    AbsParser.INTEGER pinteger -> prPrec i 0 (concatD [prt 0 pinteger])
    AbsParser.STRING str -> prPrec i 0 (concatD [printString str])
    AbsParser.CHAR c -> prPrec i 0 (concatD [prt 0 c])
    AbsParser.DOUBLE d -> prPrec i 0 (concatD [prt 0 d])
