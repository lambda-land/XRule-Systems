## File generated by the BNF Converter (bnfc 2.9.4).

# Makefile for building the parser and test program.

GHC        = ghc
HAPPY      = happy
HAPPY_OPTS = --array --info --ghc --coerce
ALEX       = alex
ALEX_OPTS  = --ghc

# List of goals not corresponding to file names.

.PHONY : all clean distclean

# Default goal.

all : TestCore

# Rules for building the parser.

AbsCore.hs LexCore.x ParCore.y PrintCore.hs TestCore.hs : Core.cf
	bnfc --haskell --functor --generic Core.cf

%.hs : %.y
	${HAPPY} ${HAPPY_OPTS} $<

%.hs : %.x
	${ALEX} ${ALEX_OPTS} $<

TestCore : AbsCore.hs LexCore.hs ParCore.hs PrintCore.hs TestCore.hs
	${GHC} ${GHC_OPTS} $@

# Rules for cleaning generated files.

clean :
	-rm -f *.hi *.o *.log *.aux *.dvi

distclean : clean
	-rm -f AbsCore.hs AbsCore.hs.bak ComposOp.hs ComposOp.hs.bak DocCore.txt DocCore.txt.bak ErrM.hs ErrM.hs.bak LayoutCore.hs LayoutCore.hs.bak LexCore.x LexCore.x.bak ParCore.y ParCore.y.bak PrintCore.hs PrintCore.hs.bak SkelCore.hs SkelCore.hs.bak TestCore.hs TestCore.hs.bak XMLCore.hs XMLCore.hs.bak ASTCore.agda ASTCore.agda.bak ParserCore.agda ParserCore.agda.bak IOLib.agda IOLib.agda.bak Main.agda Main.agda.bak Core.dtd Core.dtd.bak TestCore LexCore.hs ParCore.hs ParCore.info ParDataCore.hs Makefile


# EOF
